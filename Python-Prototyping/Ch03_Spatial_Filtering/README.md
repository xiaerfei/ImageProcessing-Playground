# Ch03 灰度变换与空间滤波(阶段 2,第 4~7 周,全书核心)

> 目标:掌握图像增强的核心手段(点变换、直方图、平滑、锐化),滤镜开发的基础。

## 阅读范围

- 精读 3.1~3.6:基本灰度变换(3.2)、直方图处理(3.3)、空间滤波基础(3.4)、平滑(3.5)、锐化(3.6)
- 泛读 3.7~3.8

## 核心知识点

- **基本灰度变换**:反色、对数变换(扩展暗部)、幂律 (Gamma) 校正、分段线性
- **直方图处理**:均衡化(CDF 映射)、直方图匹配、局部增强(CLAHE)
- **空间滤波基础**:卷积 vs 相关、核 (Kernel)、边界填充(zero/replicate/reflect)、**可分离核**(O(k²) → O(2k))
- **平滑(低通)**:盒式滤波、高斯滤波;中值滤波(非线性,去椒盐噪声)
- **锐化(高通)**:拉普拉斯算子、Unsharp Masking、Sobel 梯度

## 脚本清单(按学习顺序)

| 脚本 | 内容 |
| :--- | :--- |
| `00_intensity_doc_figures.py` | 重新生成 intensity-and-grayscale.md 的 4 张配图(非课程内容,是配图的出处) |
| `01_linear_transform.py` | 3.2 线性变换 s = a·r + b:两个旋钮、截断不可逆、代价曲线 |
| `02_negative_transform.py` | 3.2 图像反转:LUT 三种写法对齐、自逆、直方图镜像、彩色反转偏色 |
| `03_log_transform.py` | 3.2 对数变换:+1 与 ×c 的作用、归一化的坑、暗图提亮与反例 |
| `04_gamma_transform.py` | 3.2 幂律(伽马):γ 旋钮、与对数的关系、不截断但会合并灰阶、伽马校正、sRGB 的直线段;**γ>1 的正经身份是解码**,以及在哪个域做运算的代价(条纹缩小 128 vs 188) |
| `05_piecewise_linear.py` | 3.2 分段线性:对比度拉伸(三段折线)、灰度级分层;转折点怎么选、压平与梳齿的代价、r1=r2 退化成阈值化 |
| `06_bit_planes.py` | 3.2 比特平面:无损分解、用「与邻居同值」量化高位/低位、位深削减与伪轮廓、LSB 水印及其脆弱性 |
| `10_histogram_equalization.py` | 3.3 从零手写直方图均衡化:教材小例子手算、对齐 `cv2.equalizeHist`(**必须减 cdf_min**)、摊开的代价是灰阶被合并、保序与幂等、噪声放大与局部失效 |
| `11_histogram_photography_checks.py` | 3.3 摄影视角:曝光诊断、ETTR、影调分类 |
| `12_contrast_measures.py` | 3.3 对比度怎么统计:标准差、百分位跨度、自动色阶 |
| `19_histogram_basics_figures.py` | 直方图基础篇的 2 张配图:直方图/PDF/CDF 三种形态与「柱子高→增益大」那条链子、同一批像素在线性域与 gamma 域的对照 |
| `18_histogram_matching.py` | 直方图规定化:手算小图看清「查 CDF」这一步、为什么对不准(桶拆不开)、多机位色调对齐实测(只匹配 Y vs 逐通道) + 2 张配图 |
| `13_clahe_walkthrough.py` | 3.3 CLAHE 从零拆解:全局 → AHE → 插值 → 限幅 |
| `14_interactive_intensity.py` | 可拖滑块的灰度变换实验台(图像/直方图/曲线三联动) |
| `15_histogram_deep_dive.py` | 3.3 深入验证六则:直方图/PDF/CDF、CDF 为什么能摊平、空间信息丢失、gamma 域 vs 线性域、规定化、彩色分通道均衡 |
| `16_equalize_vs_clahe.py` | 3.3 全局均衡化 vs CLAHE:1 张表 vs 64 张表、局部对比度才是「细节」的指标、噪声放大 12.4× vs 2.7×、clipLimit 是均衡化没有的力度旋钮 |
| `17_clahe_from_scratch.py` | 3.3 手写 CLAHE,**与 `cv2.createCLAHE` 逐像素相同**;重点讲块间双线性插值:LUT 属于块中心、边界 clamp 自动退化、台阶 vs 折线、OpenCV 的补边怪癖 |
| `20_convolution_basics.py` | 3.4 空间滤波基础:塌缩不成 LUT、**`filter2D` 做的是相关不是卷积**(冲激响应一试便知)、只有卷积有交换律、五种补边实测、可分离(秩=1,15×15 快约 24 倍;⚠️ k≥9 时 `filter2D` 改用 DFT)、`ddepth=-1` 吃掉 45.3% 的像素 |
| `21_conv_engine.py` | 3.4 **不调库自己写卷积引擎**:补边=换一套下标(三种模式与 `copyMakeBorder` 逐像素相同)、四重循环版与 `filter2D` **0 差别**、「让图挪而不是让印章挪」的移位累加版快 **114 倍**、手写可分离(横一遍+竖一遍);两张图解 |
| `22_smoothing.py` | 3.5 平滑四件套:盒式的点响应是**方块**、高斯是**圆**;⚠️ **σ 按勾股定理叠加**(3 再 4 = 5,当成 7 差 37 级);盒式连做 2 次≈高斯;核取 `6σ+1`(σ=3 配 3×3 切掉 61.5%);椒盐→中值(29.5 vs 23.2 dB)、高斯噪声→高斯/双边;代价表 |
| `23_sharpening.py` | 3.6 锐化:一阶 vs 二阶导数的一维演示;拉普拉斯 = 横竖两个二阶导相加(−4 = −2 + −2);二阶能直接加、一阶必须开方(135° 边 gx+gy = −0.00);⚠️ 八邻域 −8 没折算对角 √2 步长,各向同性反不如四邻域(9.75% vs 8.94%,折算版 −6 是 7.92%);拐点 = 一阶峰顶 = 二阶零点,|二阶| 最大的两处都不是边(halo 就长在那儿);⚠️ 「零点在过渡带正中」只对对称边成立,不对称边实测偏 +9.563 / −10.563 格;锐化核 = 单位核 − 拉普拉斯(符号自检);USM 三步;⚠️ 小半径 USM ≈ 拉普拉斯锐化(相关 0.9918);过冲 [50,200]→[−100,350];噪声放大 5.4× vs 1.86×;|gx|+|gy| 省开方;Prewitt/Sobel/Scharr 方向误差 0.549°/0.275°/0.068° |
| `24_edge_detection.py` | 边缘检测:**手写 Canny 四步**(与 `cv2.Canny` F1 0.901)、LoG 零交叉;并给一篇知乎文章纠错并实测:「核的和为 0 保证亮度不变」(实测均值 129.1→0.00)、「NMS 后置 1 得二值图」(实测跨 423 档,置 1 则双阈值失效)、「NMS 要求 8 邻域最大」(误删 84.3%)、「滞后阈值代价过大常舍去」(栈迭代 10 ms);Roberts 定位偏 0.5 像素 |
| `25_gradient_direction.py` | 梯度方向为什么垂直于边:造一批走向已知的边量夹角(五个角度恒为 90°),外加「灰度当海拔」的示意与平坦区方向全是噪声的对照 |

> **编号规则**(号段留了空位,以后插脚本不用推别人):
>
> | 号段 | 归属 |
> | :--- | :--- |
> | `00` | 文档配图生成,不是课程内容 |
> | `01`~`09` | 3.2 灰度变换 |
> | `10`~`19` | 3.3 直方图与对比度 |
> | `20`~ | 3.4~3.6 空间滤波(卷积引擎、高斯、中值、Sobel、拉普拉斯),文档见 [05-spatial-filtering/](../../Documents/05-spatial-filtering/)(原理篇 + 平滑篇) |
>
> 段内编号即阅读顺序。3.2 还差阈值化一篇,已留好 `07`;3.3 的 `18`~`19` 还空着(规定化、局部统计增强),备忘见 [`Documents/03-histogram/04-further-topics.md`](../../Documents/03-histogram/07-further-topics.md)。

## 实践任务

- [ ] 反色 / 对数 / Gamma / 分段线性变换,输出对比图
- [x] 手写直方图均衡化,对比 `cv2.equalizeHist`;再试 CLAHE(`10_histogram_equalization.py`、`13_clahe_walkthrough.py`)
- [ ] 手写通用 2D 卷积引擎(三种边界模式),与 `cv2.filter2D` 对齐到 1e-6
- [ ] 手写高斯核生成 + 可分离卷积,验证结果一致并对比耗时
- [ ] 中值滤波去椒盐噪声,对比高斯滤波
- [ ] Sobel 梯度幅值图;拉普拉斯锐化;Unsharp Masking

## 验收问题

- 卷积和相关的区别?`cv2.filter2D` 做的是哪个?
- 直方图均衡化为什么用 CDF?什么时候效果差?
- 高斯核可分离的原理?
- Unsharp Masking 三步与拉普拉斯锐化的关系?
