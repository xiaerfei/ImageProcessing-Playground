# 学习路线图(ROADMAP)

- **教材**:冈萨雷斯《数字图像处理》第四版(Digital Image Processing, 4th Edition)
- **背景**:10 年 iOS/macOS 音视频/推流开发;电子信息工程本科;高数/复变/信号系统需按需重建
- **时间预算**:每周 6~8 小时(工作日 2~3 晚 × 1~1.5h 读书笔记,周末半天 3~4h 写代码),主线 18 周
- **节号说明**:以第四版英文原版目录为准,中译本个别节号可能有出入,以书的实际目录为准
- **全书知识版图**:见 [Documents/book-outline.md](Documents/book-outline.md)(12 章完整知识点 + 每章路线定位)

## 一、总体原则

1. **代码先行,理论跟进**:每个概念先跑通 Python 最小实现,再回头看书上的推导。
2. **公式当伪代码读**:双重求和 = 两层 for 循环;矩阵乘 = 一次变换。数学"按需查找,即用即补"。
3. **两条实现线,主次分明**:Python/numpy 是主线;Metal 只挑有实时价值的算法落地。
4. **每阶段有验收**:答不上验收问题就不进入下一阶段,用缓冲周补。
5. **输出驱动**:每章一篇笔记(`Documents/`),效果图存 `Assets/results/`,每周在本文件打卡。

**代码文件约定**:按周计划中给出的文件名建在对应章节目录下,前缀数字表示顺序,一个文件解决一个主题。

## 二、阶段总览

| 阶段 | 周次 | 章节 | 主题 |
| :--- | :--- | :--- | :--- |
| 0 | 第 1 周 | — | 环境搭建与热身 |
| 1 | 第 2~3 周 | 第 1、2 章 | 图像基础:采样量化、插值与几何变换 |
| 2 | 第 4~7 周 | 第 3 章 | 灰度变换与空间滤波(全书核心) |
| 3 | 第 8~9 周 | 第 6 章 | 彩色图像处理 + 视频色彩工程 |
| — | 第 10 周 | — | 缓冲周 + 中期项目 |
| 4 | 第 11~14 周 | 第 4 章 | 频域处理(先数学回血) |
| 5 | 第 15~17 周 | 第 9、10 章 | 形态学与图像分割 |
| — | 第 18 周 | — | 毕业项目:实时绿幕抠图 |
| 选修 | 其后 | 第 5 / 8 / 12 章 | 去噪 / 图像压缩 / 深度学习入门 |

---

## 三、逐周计划

### ▸ 第 1 周(阶段 0):环境搭建与热身

**工作日晚**:
- 搭 Python 环境:`python3 -m venv .venv` + `pip install -r Python-Prototyping/requirements.txt`
- 从 imageprocessingplace.com 下载标准测试图,放入 `Assets/test-images/`
- 写 `Ch01_02_Fundamentals/00_hello_image.py`:
  - 读图并打印 `shape` / `dtype`(注意 `cv2.imread` 返回的是 **BGR** 不是 RGB)
  - 分离通道、把某通道置零、matplotlib 显示、保存结果

**周末**:
- 写 `Ch06_Color_Processing/00_yuv_warmup.py`(热身,不看书):
  - 先用纯色块探测 `cv2` 的 I420 转换用什么系数/range(实测:BT.601 + video range)
  - 手写 Kr/Kb 参数化的 YUV→RGB,匹配配置与 `cv2.COLOR_YUV2RGB_I420` 误差 ±1 以内
  - 故意用 BT.709 解码,可视化偏色 —— 第 8 周 pitfalls 实验的预告
- Metal:建最小 macOS App(`Metal-Implementation/DIPMetalEngine/`),加载图片 → MTKView 显示

**本周产出**:2 个脚本 + App 骨架 + 笔记《图像的内存布局:HWC、stride、BGR/RGB、YUV planar/semi-planar》

- [x] 第 1 周完成(2026-07-30,环境 + 测试图 + 双脚本 + Metal 骨架)

### ▸ 第 2 周(阶段 1):采样、量化与像素关系

**读**:第 1 章泛读(1 晚);2.4 精读(采样与量化、空间/灰度分辨率);2.5 精读(像素间关系);2.1~2.3 泛读

**知识点**:
- 采样(决定空间分辨率)与量化(决定灰度级数);欠采样 → 摩尔纹/锯齿
- 位深下降 → **伪轮廓**(false contouring):平滑渐变区出现台阶
- 邻域:N4(上下左右)、N8(含对角)、m 邻接(消除 8 邻接的歧义)
- 邻接 → 通路 → 连通 → 区域 → 边界 的递进定义
- 距离度量:欧氏 D_e、城市街区 D4(菱形等距线)、棋盘 D8(方形等距线)

**写** `Ch01_02_Fundamentals/01_sampling_quantization.py`:
- 图像逐级降采样(1/2、1/4、1/8)再放大回原尺寸,观察块状马赛克
- 位深 8→6→4→2→1 bit 逐级量化(`(img >> n) << n`),找到伪轮廓开始明显的位深

**本周产出**:两组对比图存 `Assets/results/`;笔记回答:手机屏幕看不出 6bit 和 8bit 差别的场景是什么?什么场景一眼看出(渐变天空)?

- [ ] 第 2 周完成

### ▸ 第 3 周(阶段 1):插值、几何变换与算术运算

**读**:2.4 的插值部分精读;2.6 精读算术运算与空间几何变换,其余小节泛读

**数学补给**:3B1B《线性代数的本质》第 1~4 集(向量、线性变换、矩阵乘法)

**知识点**:
- 最近邻(取整,块状)、双线性(4 邻点加权,平滑)、双三次(16 邻点,更锐但可能过冲)
- 仿射变换 = 2×3 矩阵:旋转/缩放/平移/剪切的统一表示
- **逆向映射**:遍历目标像素,反算源坐标再插值 —— 正向映射会留洞
- 算术运算:多帧平均降噪(噪声标准差 ∝ 1/√N)、图像相减(差异检测)

**写**:
- `02_interpolation.py`:手写最近邻 + 双线性放大 4×,与 `cv2.resize` 三种模式对比,算 PSNR
- `03_affine_transform.py`:构造"旋转 30° + 缩放 0.7"矩阵,逆向映射 + 双线性采样实现;与 `cv2.warpAffine` 对比
- `04_frame_averaging.py`:同一图加高斯噪声,N=1/4/16/64 张取平均,画噪声标准差随 N 变化曲线,验证 1/√N

**Metal**:用 sampler 的 linear filtering 做缩放,与手写双线性输出对比

**阶段 1 验收**(答不上回头补):
- 位深从 8 降到 4 会发生什么?名词是什么?
- 双线性为什么比最近邻平滑?双三次为什么可能在边缘过冲?
- 几何变换为什么必须逆向映射?
- 多帧平均为什么能降噪?对运动画面直接平均会怎样(拖影 → 引出运动补偿)?

- [ ] 第 3 周完成

### ▸ 第 4 周(阶段 2):基本灰度变换

**读**:3.1 精读(空域处理框架:点运算 vs 邻域运算);3.2 精读
**速查**:[Documents/intensity-and-grayscale.md](Documents/intensity-and-grayscale.md) —— 四种变换的曲线与 LUT 要点

**知识点**:
- 反色:`s = 255 - r`(X 光片增强类场景)
- 对数变换:`s = c·log(1+r)`,c = 255/log(256) —— 压缩高动态、扩展暗部(频谱显示会用到)
- 幂律/Gamma:`s = 255·(r/255)^γ`;γ<1 提亮暗部,γ>1 压暗 —— 与显示器 sRGB gamma、视频 OETF 的关系
- 分段线性:对比度拉伸、灰度分层
- **点运算的工程本质:256 项 LUT 查表** —— 与你工作中"一切调色皆 LUT"接上

**写** `Ch03_Spatial_Filtering/01_intensity_transforms.py`:
- 实现以上四类变换,统一封装成"生成 LUT → `cv2.LUT` 应用"的模式
- 对一张暗图和一张过曝图分别调参,输出 2×4 对比图板

**Metal(可选)**:gamma 调节 shader(一行 `pow`),滑杆实时调 γ

**本周自测**:为什么相机 raw 要做 gamma 编码再存 8bit(感知均匀、暗部量化位分配)?

- [ ] 第 4 周完成

### ▸ 第 5 周(阶段 2):直方图处理

**读**:3.3 精读

**速查**:[Documents/histogram-transform.md](Documents/histogram-transform.md) —— 直方图/均衡化/CLAHE/规定化的大白话入门
**摄影视角**:[Documents/histogram-reading.md](Documents/histogram-reading.md) —— 读懂直方图形态、影调分类、ETTR;第九节给出「形态 → 该用什么变换」对照表

**知识点**:
- 直方图 = 灰度分布统计;亮图/暗图/低对比度图的直方图形态
- **均衡化**:用 CDF 作映射函数 `s = (L-1)·CDF(r)`,把分布"摊平"以拉开对比度
- 均衡化后直方图并非严格平坦(离散灰度级合并,只能变稀不能拆分)
- 直方图匹配(规定化):把分布映射成指定形状(两次均衡化的复合)
- 局部/自适应:分块均衡 + 插值 = CLAHE(clip limit 防止噪声放大)

**写**:
- `02_histogram_equalization.py`:手写(numpy 统计直方图 → 累加 CDF → 查表映射),与 `cv2.equalizeHist` 结果一致;画均衡化前后直方图对比
- `03_histogram_matching.py`:实现规定化,把 A 图的直方图匹配到 B 图
- 用 `cv2.createCLAHE` 对逆光人像做局部增强,对比全局均衡的过增强问题

**本周自测**:全局均衡化对"大面积天空 + 小块暗部主体"的图为什么效果差?CLAHE 的 clip limit 在防什么?

- [ ] 第 5 周完成

### ▸ 第 6 周(阶段 2):空间滤波基础与平滑

**读**:3.4 精读(务必吃透)、3.5 精读

**知识点**:
- **相关 vs 卷积**:卷积先把核旋转 180°;对称核两者等价;`cv2.filter2D` 做的是相关
- 边界策略:zero / replicate / reflect,各自的边缘伪影
- 盒式滤波(均值)vs 高斯:高斯核 `G = exp(-(x²+y²)/2σ²)` 归一化;权重随距离衰减,无盒式的方向性伪影
- σ 与核尺寸经验:ksize ≈ 6σ 取奇数
- **可分离性**:2D 高斯 = 水平 1D × 垂直 1D(外积),复杂度 O(k²)→O(2k)
- 中值滤波:非线性、保边,椒盐噪声克星(均值/高斯对椒盐无力)

**写**:
- `04_convolution_engine.py`:手写通用 2D 卷积(支持三种边界),与 `cv2.filter2D`(同 borderType)误差 < 1e-6
- `05_smoothing.py`:手写高斯核生成 + 可分离卷积,验证与直接 2D 卷积结果一致;`%timeit` 对比 k=15 时耗时
- `06_median_filter.py`:加 5% 椒盐噪声,中值 vs 高斯 vs 均值三方对比图

**本周自测**:为什么高斯核可分离(外积分解)?中值滤波为什么不能用卷积表达?

- [ ] 第 6 周完成

### ▸ 第 7 周(阶段 2):锐化 + Metal 落地

**读**:3.6 精读;3.7~3.8 泛读

**知识点**:
- 一阶导(梯度):Sobel 核(平滑+差分的组合),梯度幅值 `√(gx²+gy²)` → 边缘强度图
- 二阶导:拉普拉斯(4 邻域 / 8 邻域两种核),各向同性;锐化 = 原图 − 拉普拉斯(核中心为负时)
- **Unsharp Masking**:模糊 → 原图减模糊得细节层 → 原图 + k×细节层;k>1 即高提升滤波(highboost)
- 拉普拉斯锐化 ≈ k 固定的 unsharp masking(高斯代替均值时)

**写** `07_sharpening.py`:拉普拉斯两种核对比、unsharp mask 的 k∈{0.5, 1, 2, 5} 扫描(观察过锐化光晕)、Sobel 梯度幅值图

**Metal**(本周重头):
- 通用 3×3 卷积 compute shader(核系数作 uniform)
- 可分离高斯两 pass,与 `MPSImageGaussianBlur` 对照结果与耗时
- Sobel 边缘检测实时滤镜(摄像头)

**阶段 2 验收**:
- `filter2D` 是相关还是卷积?对 Sobel 核有影响吗?
- 均衡化为什么用 CDF?
- 可分离卷积省在哪?15×15 核省多少倍乘法?
- Unsharp masking 三步?与拉普拉斯锐化的关系?
- (工程)Metal 里在 sRGB 空间做模糊为什么偏暗?

- [ ] 第 7 周完成

### ▸ 第 8 周(阶段 3):颜色模型与视频色彩工程

**读**:6.1~6.2 精读;6.3 泛读
**速查**:[Documents/intensity-and-grayscale.md](Documents/intensity-and-grayscale.md) 第三、四节 —— 各图像类型取亮度、Y/V/L*/L 的区别

**知识点**:
- 三原色/三补色、色度图;RGB 立方体、CMY(K) 印刷
- HSI/HSV:把"是什么颜色(H)/有多纯(S)/有多亮(I/V)"解耦;HSV 与 HSI 的差别
- **书外必补(推流核心)**:
  - 亮度系数三套:BT.601(0.299/0.587/0.114)、BT.709(0.2126/0.7152/0.0722)、BT.2020(0.2627/0.6780/0.0593)
  - full range vs video range(Y: 16~235, C: 16~240)
  - 色度下采样 4:4:4 / 4:2:2 / 4:2:0;I420(三平面)vs NV12(Y + UV 交错)内存布局

**写**:
- `01_rgb_hsv.py`:手写 RGB↔HSV,与 OpenCV 对比(注意 OpenCV 的 H 是 0~180)
- `02_yuv_pitfalls.py`(高价值实验):
  - 用 601 矩阵编码、709 矩阵解码,输出偏色对比图(肤色最明显)
  - video range 数据按 full range 解读,输出"发灰"对比图
  - 4:2:0 下采样再上采样,放大看色度边缘的溢色

**本周自测**:为什么 4:2:0 砍掉 3/4 色度数据人眼却不敏感(视锥细胞对亮度分辨率 >> 色度)?发灰的画面九成是什么 bug?

- [ ] 第 8 周完成

### ▸ 第 9 周(阶段 3):全彩处理与 3D LUT

**读**:6.4~6.6 精读;6.7 留到第 16 周回看

**知识点**:
- 彩色变换:补色、彩色分层、色调旋转、饱和度调整
- 平滑/锐化彩色图:逐 RGB 通道做 vs 只在亮度通道做(后者不引入偏色)
- 白平衡:灰度世界假设(三通道均值应相等)
- **3D LUT**:任意色彩映射的通用容器;17³/33³ 格点 + 三线性插值;.cube 文件格式

**写**:
- `03_color_enhancement.py`:亮度通道均衡 vs RGB 三通道分别均衡(输出偏色对照);饱和度增益;灰度世界白平衡
- `04_lut3d.py`:解析 .cube 文件,手写三线性插值应用到图像;网上找一个胶片风 LUT 验证

**Metal**:
- 3D LUT shader:LUT 存 3D 纹理,硬件三线性插值,一次采样完成调色
- 饱和度/色温/对比度实时滑杆

**阶段 3 验收**:
- 视频为什么用 YUV?4:2:0 省多少带宽?
- RGB 三通道分别均衡为什么偏色?
- 601/709 混用、range 错配的画面症状分别是什么?
- 3D LUT 为什么能表达几乎任何调色(任意映射的采样+插值)?

- [ ] 第 9 周完成

### ▸ 第 10 周:缓冲周 + 中期项目「实时调色滤镜 App」

- 前 9 周欠账全部还清,过一遍三个阶段的验收问题。
- **中期项目需求**(只用已学知识):
  1. 摄像头采集 → Metal 渲染预览
  2. YUV→RGB 在 shader 中完成(自选 601/709,做对!)
  3. Gamma / 对比度 / 饱和度三个实时滑杆
  4. 3D LUT 滤镜切换(至少 2 个 .cube)
  5. 实时直方图叠加显示(可抽帧计算)
  - **指标**:1080p 预览 30fps 不掉帧
- **产出**:效果 GIF 存 `Assets/results/`,笔记记录性能数据与踩坑

- [ ] 第 10 周完成(中期项目验收)

### ▸ 第 11 周(阶段 4):数学回血(不开书)

**任务与时长**:
- 复数四则 + 欧拉公式 `e^{jθ} = cosθ + j·sinθ`,几何意义 = 单位圆上转角 θ(1 晚,维基/任意教程)
- 3B1B《傅里叶变换的本质》看两遍(1 晚)
- 3B1B《线性代数的本质》"基变换 / 点积"两集 —— 建立"DFT = 把信号投影到一组旋转基上"的视角(1 晚)

**写** `Ch04_Frequency_Domain/01_dft_1d.py`:
- 朴素 O(N²) DFT:`X[k] = Σ x[n]·e^{-j2πkn/N}`(两层循环直译)
- 合成信号 `sin(2π·5t) + 0.5·sin(2π·12t)`(N=128),幅度谱应在 k=5、12 出峰,幅值比 2:1
- 与 `numpy.fft.fft` 对齐;实现逆 DFT 还原信号

**本周自测**:X[k] 的 k 对应的物理频率是多少(k·fs/N)?为什么实信号的频谱共轭对称(负频率不含新信息)?

- [ ] 第 11 周完成

### ▸ 第 12 周(阶段 4):2D DFT 与频谱阅读

**读**:4.2、4.4 精读(以 1D 巩固直觉);4.3 泛读(采样定理 —— 联系你做过的音频采样率、视频降采样);4.5~4.6 精读

**知识点**:
- 2D DFT = 先对每行做 1D,再对每列做 1D(可分离)
- 幅度谱(能量在哪些频率)与相位谱(结构在哪);**相位主导图像结构**
- `fftshift`:把零频移到中心;显示用 `log(1+|F|)`
- 性质:平移 → 只变相位;旋转 → 频谱同角度旋转;共轭对称

**写** `02_spectrum_visualization.py`:
- 对纹理图/人像/纯色块图分别显示频谱,总结"频谱长相"与图像内容的关系
- 图像旋转 45° → 验证频谱跟着转
- **幅相交换实验**:A 的幅度 + B 的相位重建,验证"看起来像 B"

**本周自测**:频谱中心亮斑是什么?一条穿过中心的亮线对应图像里什么(垂直于它的边缘/条纹)?

- [ ] 第 12 周完成

### ▸ 第 13 周(阶段 4):频域滤波

**读**:4.7~4.9 精读

**知识点**:
- 频域滤波标准流程:**零填充到 2M×2N →(乘 (-1)^(x+y) 或 fftshift)中心化 → 乘 H(u,v) → IDFT 取实部 → 反中心化 → 裁剪**
- 为什么要零填充:DFT 隐含周期延拓,不填充会发生卷绕(wraparound)污染
- 低通:理想(硬截止 → **振铃**,吉布斯现象)、巴特沃斯(阶数控制过渡带)、高斯(无振铃)
- 高通 = 1 − 低通;频域拉普拉斯;高频强调
- **卷积定理**:空域卷积 ⇔ 频域乘积;核大时频域更快

**写**:
- `03_lowpass_filters.py`:ILPF/BLPF/GLPF 同截止频率对比(振铃可视化);截止半径扫描观察模糊程度
- `04_highpass_sharpen.py`:高斯高通锐化
- `05_convolution_theorem.py`:空域高斯卷积 vs 频域乘高斯传递函数,结果对齐(注意 padding 一致)

**本周自测**:理想低通的振铃从哪来(sinc 的旁瓣)?什么时候频域滤波比空域快(核大、或同尺寸多次滤波复用 FFT)?

- [ ] 第 13 周完成

### ▸ 第 14 周(阶段 4):选择性滤波 + 收尾

**读**:4.10 精读(带阻/带通、陷波);4.11 泛读(FFT 分治思想,不抠细节)

**写**:
- `06_notch_filter.py`:给图像人为叠加周期条纹(加一个 2D 正弦),频谱上找到亮点对,设计陷波滤波器精确去除,对比空域方法的无力
- 整理阶段 4 笔记:一页纸《频域速查:流程、三种低通、陷波》

**阶段 4 验收**:
- 频域滤波完整流程六步能默写吗?零填充防什么?
- 三种低通的取舍?
- DCT 与 DFT 的关系?为什么压缩用 DCT(实数、能量集中、边界连续性好)—— 选修 B 伏笔
- 幅度谱和相位谱哪个更"重要"?实验依据?

- [ ] 第 14 周完成

### ▸ 第 15 周(阶段 5):形态学

**读**:9.1~9.3 精读;9.5 精读(边界提取、孔洞填充、连通分量);灰度形态学泛读

**知识点**:
- 结构元(SE):形状与大小决定操作效果;方形 vs 圆形 SE
- 腐蚀(白区收缩、去小白点)/ 膨胀(白区扩张、填小黑洞)—— 对偶关系
- **开运算**(腐→膨):去孤立噪点、断细连接,保主体形状
- **闭运算**(膨→腐):填小孔、接断裂
- 形态学梯度 = 膨胀 − 腐蚀(边界);顶帽/底帽(不均匀光照校正)
- 连通分量标记

**写**:
- `01_morphology_basics.py`:手写二值腐蚀/膨胀(SE 滑窗 min/max),组合出开/闭,与 `cv2.morphologyEx` 对比
- `02_mask_cleanup.py`:对一张带噪二值 mask(自造:阈值分割结果加椒盐),开+闭组合清理干净
- `03_connected_components.py`:用 `cv2.connectedComponents` 数硬币/物体个数

**本周自测**:先开后闭 vs 先闭后开结果一样吗?为什么?SE 尺寸选大了会怎样?

- [ ] 第 15 周完成

### ▸ 第 16 周(阶段 5):阈值分割

**读**:10.3 精读(Otsu 重点);10.4~10.5 泛读(区域生长、k-means/超像素 —— 第四版新增);回看 6.7(基于彩色的分割)

**知识点**:
- 全局阈值迭代法;**Otsu**:遍历阈值 t,最大化类间方差 `σ²(t) = w0·w1·(μ0−μ1)²`
- Otsu 的前提:直方图近似双峰;光照不均时失效 → 分块自适应阈值
- 彩色分割:在 HSV/YCbCr 空间对色度设阈值(绿幕的原理)

**写**:
- `01_otsu.py`:手写 Otsu(一层循环遍历 256 个阈值),与 `cv2.threshold(..., THRESH_OTSU)` 得到相同阈值
- `02_adaptive_threshold.py`:对光照不均的文档图,全局 Otsu vs `cv2.adaptiveThreshold` 对比
- `03_color_keying.py`(绿幕预演):HSV 空间绿色范围阈值 → 二值 mask → 上周的形态学清理

**本周自测**:Otsu 在前景占比极小(1%)的图上为什么容易失败?自适应阈值的窗口大小怎么选?

- [ ] 第 16 周完成

### ▸ 第 17 周(阶段 5):边缘检测与 Canny

**读**:10.2 精读(梯度算子 → LoG → **Canny 完整流程**);10.8 的运动分割一节建议看(与视频相关)

**知识点**:
- Canny 四步:① 高斯平滑(σ 控制尺度)② Sobel 求梯度幅值+方向 ③ **非极大值抑制**(沿梯度方向只留局部最大 → 单像素宽边缘)④ **双阈值 + 滞后连接**(强边缘直接保留,弱边缘仅当连着强边缘才保留)
- 为什么双阈值优于单阈值:单阈值要么断边要么噪声多

**写** `04_canny_steps.py`:
- 分四步实现,输出 2×3 面板图:原图 → 平滑 → 梯度幅值 → NMS 后 → 双阈值 → 最终
- 与 `cv2.Canny` 对比(不必逐像素一致,趋势对即可);扫描高低阈值比(2:1 vs 3:1)

**阶段 5 验收**:
- 开/闭运算各治什么?顺序敏感吗?
- Otsu 的目标函数是什么?何时失效?
- NMS 在解决什么问题?滞后连接为什么能兼顾"少断边"和"少噪声"?

- [ ] 第 17 周完成

### ▸ 第 18 周:毕业项目「实时绿幕抠图」

**Python 原型** `Ch10_Segmentation/05_greenscreen_proto.py`:
1. YCbCr 或 HSV 色度键控 → 初始 mask
2. 开/闭运算清理噪点与孔洞
3. mask 高斯羽化(边缘 alpha 渐变,消硬边)
4. alpha 合成到新背景;处理绿色溢色(spill suppression:压制边缘像素的 G 分量)

**Metal 实时版**:
- pipeline:摄像头帧 → 键控 compute shader → 形态学 pass(可用可分离 min/max 近似)→ 羽化(小半径高斯)→ 合成
- **指标**:1080p 60fps;记录每 pass 耗时(GPU capture)

**产出**:前后对比 GIF 进 README;`Documents/` 写总结:踩坑、性能数据、与 Vision 人像分割(深度学习路线)的效果对比感受 → 引出选修 C

- [ ] 第 18 周完成 —— **主线毕业** 🎓

---

## 四、选修模块(主线毕业后按需选择)

### 选修 A:第 5 章 噪声模型与去噪(约 2 周)

- **第 1 周**:读 5.1~5.3。生成高斯/椒盐/均匀噪声并观察平坦区直方图特征(`01_noise_models.py`);对比算术/几何/逆谐波均值滤波(`02_mean_filters.py`,注意逆谐波 Q 的正负对椒/盐的选择性);自适应中值滤波应对高密度椒盐
- **第 2 周**(书外延伸,工程价值高):手写**双边滤波**(空间高斯 × 值域高斯,保边平滑 = 磨皮基础,`03_bilateral.py`);了解非局部均值;笔记梳理视频时域降噪思路(运动检测 + 时域加权,静区多用时域、动区多用空域)
- 适合:关心弱光推流画质

### 选修 B:第 8 章 图像压缩(约 2~3 周)—— 对推流开发者价值最高

- **第 1 周**:读 8.1(三类冗余、信息熵、保真度准则)+ 霍夫曼编码。`01_entropy.py`:计算图像熵,对比 PNG/JPEG 实际压缩率
- **第 2 周**:读块变换编码一节(JPEG 全流程)。`02_dct_jpeg.py`:手写 8×8 DCT → 标准量化表(可乘系数模拟质量因子)→ 反变换,可视化不同质量下的**块效应**;观察量化表为什么高频格子数值更大
- **第 3 周**:读预测编码一节(DPCM、**运动补偿**)。`03_predictive.py`:实现相邻像素差分编码,对比差分前后的熵;笔记:把 I/P/B 帧、码率控制、量化参数 QP 这些日常名词接到本章理论上
- 收益:理解 H.264/HEVC 为什么这么设计

### 选修 C:第 12 章 神经网络与深度学习入门(约 2 周)

- 读第 12 章后半(感知机 → 多层网络与反向传播 → 深度卷积网络)
- 关键连接点:**CNN 的卷积层 = 第 3 章的空间滤波,只是核由学习得到**;第一层学出的核往往就是边缘检测算子(和 Sobel 长得像)
- 实践:用 Vision / CoreML 跑一次人像分割,与毕业项目的传统绿幕方案对比效果与算力
- 意义:通往现代抠图/分割方案的桥

### 明确跳过

第 7 章小波与其他变换、第 11 章特征提取(SIFT 等)—— 需要时当手册回来查。

---

## 五、通用避坑清单

1. **uint8 回绕**:numpy 里 uint8 的 200+100 = 44。所有运算先 `astype(np.float64)`,输出前 `np.clip(x, 0, 255).astype(np.uint8)`。
2. **BGR vs RGB**:`cv2.imread` 是 BGR,matplotlib 显示是 RGB,混了颜色就反。
3. **卷积 vs 相关**:`cv2.filter2D` 做的是相关;对称核无所谓,Sobel 这类非对称核要清楚差别。
4. **边界处理**:手写结果与 OpenCV 的差异往往只在边缘 —— 对比前先统一 borderType。
5. **频谱显示**:`log(1 + |F|)` + `fftshift`,否则一片黑;频域滤波不零填充会卷绕。
6. **sRGB 与线性空间**:Metal 的 `_srgb` 纹理采样返回线性值;在 gamma 空间做模糊结果偏暗。
7. **YUV range 与系数**:video range(Y: 16~235)与 full range 矩阵不同;BT.601/709 混用 → 肤色偏差;range 错配 → 画面发灰或黑白剪切。推流最常见的两个色彩 bug。
8. **别过度 Metal 化**:Python 主线不能停,Metal 只做各周列出的任务。

## 六、资源清单

| 资源 | 用途 |
| :--- | :--- |
| 《数字图像处理(第四版)》 | 主教材 |
| imageprocessingplace.com | 教材官方站,下载标准测试图 |
| 3B1B《线性代数的本质》《傅里叶变换的本质》 | 数学直觉(B站/YouTube) |
| OpenCV-Python 官方 Tutorials | 对照参考实现 |
| MetalPerformanceShaders(MPS) | 现成 GaussianBlur/Histogram/Sobel,自写 shader 的对照基准 |
| Accelerate / vImage | CPU 端参考基准 |
| Poynton《Digital Video and HD》(选) | 视频色彩权威(YUV/range/色域一手资料) |
| DIPUM 配套代码(选) | 冈萨雷斯官方实践指南 |
