# 学习路线图(ROADMAP)

- **教材**:冈萨雷斯《数字图像处理》第四版(Digital Image Processing, 4th Edition)
- **背景**:10 年 iOS/macOS 音视频/推流开发;电子信息工程本科;高数/复变/信号系统需按需重建
- **时间预算**:每周 6~8 小时(工作日晚上读书做笔记,周末集中写代码),主线约 18 周,选修另计

## 一、总体原则

1. **代码先行,理论跟进**:每个概念先跑通 Python 最小实现,再回头看书上的推导。
2. **公式当伪代码读**:双重求和 = 两层 for 循环;矩阵乘 = 一次变换。卡住的数学"按需查找,即用即补",不提前系统复习。
3. **两条实现线,主次分明**:Python/numpy 是主线(所有算法手写验证);Metal 只挑有实时渲染价值的算法落地,避免完美主义拖垮进度。
4. **每阶段有验收**:能回答验收问题、跑通验收代码才进入下一阶段;达不到就用缓冲周补。
5. **输出驱动**:每章在 `Documents/` 留一篇笔记,README 打卡,效果图存 `Assets/results/`。

## 二、阶段总览

| 阶段 | 周次 | 章节 | 主题 |
| :--- | :--- | :--- | :--- |
| 0 | 第 1 周 | — | 环境搭建与热身(YUV→RGB 建立信心) |
| 1 | 第 2~3 周 | 第 1、2 章 | 图像基础:采样量化、**插值与几何变换** |
| 2 | 第 4~7 周 | 第 3 章 | 灰度变换与空间滤波(全书核心) |
| 3 | 第 8~9 周 | 第 6 章 | 彩色图像处理 + 视频色彩工程补充 |
| — | 第 10 周 | — | 缓冲周:补欠账 + 中期项目(实时调色滤镜) |
| 4 | 第 11~14 周 | 第 4 章 | 频域处理(先专门一周数学回血) |
| 5 | 第 15~17 周 | 第 9、10 章 | 形态学与图像分割 |
| — | 第 18 周 | — | 毕业项目:实时绿幕抠图 |
| 选修 | 其后 | 第 5 / 8 / 12 章 | 去噪 / 图像压缩 / 深度学习入门 |

## 三、逐阶段详细计划

### 阶段 0(第 1 周):环境搭建与热身

**目标**:两条实现线都能跑起来;用最熟悉的 YUV→RGB 建立信心。

**任务**:
- Python:venv + `requirements.txt`;读一张图、访问/修改像素、matplotlib 显示;从教材官方站 imageprocessingplace.com 下载标准测试图放入 `Assets/test-images/`
- Metal:建一个最小 macOS App —— 加载图片(或摄像头帧)→ MTKView 显示(对你这是复习不是学习)
- 热身实验(不看书):用 numpy 手写 BT.709 YUV→RGB,和工作中的认知对照

**验收**:
- [ ] Python 里能把一张图的某个通道置零并显示
- [ ] Metal App 能显示一帧图像
- [ ] 手写 YUV→RGB 输出与 `cv2.cvtColor` 误差在 ±1 以内(注意 full/video range)

### 阶段 1(第 2~3 周):数字图像基础 —— 第 1、2 章

**阅读**:第 1 章泛读;第 2 章精读 2.4(采样与量化,重点插值一节)、2.5(像素间关系)、2.6(数学工具,重点算术运算与几何变换);2.1~2.3 泛读。

**数学补给**:3B1B《线性代数的本质》第 1~4 集(向量、线性变换、矩阵乘法)。

**Python 实践**:
- [ ] 手写最近邻插值和双线性插值缩放,和 `cv2.resize` 对比
- [ ] 手写仿射变换(旋转+缩放),用逆向映射实现
- [ ] 多帧平均降噪:同一图像加噪 N 次取平均,验证噪声随 N 下降(算术运算应用)

**Metal 实践**:
- [ ] 用 sampler 的 linear filtering 做缩放,对比手写双线性结果

**验收问题**:
- 空间分辨率和灰度分辨率分别由什么决定?位深从 8 位降到 4 位会出现什么现象(伪轮廓)?
- 双线性插值为什么比最近邻平滑?双三次为什么更锐?
- 几何变换为什么要用逆向映射(目标→源)而不是正向映射?

**避坑**:正向映射会产生空洞;numpy uint8 直接相加会回绕,先转 float。

### 阶段 2(第 4~7 周):灰度变换与空间滤波 —— 第 3 章(全书核心)

**阅读**:3.1~3.6 精读(3.2 基本灰度变换、3.3 直方图处理、3.4 空间滤波基础、3.5 平滑、3.6 锐化);3.7~3.8 泛读。

**周节奏**:第 4 周 3.1~3.2 + 直方图入门;第 5 周直方图均衡化/匹配;第 6 周卷积与平滑;第 7 周锐化 + Metal 落地。

**Python 实践**:
- [ ] 反色 / 对数变换 / Gamma / 分段线性,各输出对比图
- [ ] 手写直方图均衡化(CDF 映射),对比 `cv2.equalizeHist`;再试 CLAHE 观察局部增强差异
- [ ] 手写通用 2D 卷积引擎(支持 zero/replicate/reflect 三种边界),与 `cv2.filter2D` 对齐到 1e-6
- [ ] 手写高斯核生成 + 可分离卷积,验证与 2D 直接卷积结果一致并对比耗时
- [ ] 中值滤波去椒盐噪声,对比高斯滤波(体会线性 vs 非线性)
- [ ] Sobel 梯度幅值图;拉普拉斯锐化;Unsharp Masking

**Metal 实践**:
- [ ] 通用 3×3 卷积 compute shader
- [ ] 可分离高斯模糊(水平+垂直两个 pass),与 `MPSImageGaussianBlur` 对照
- [ ] Sobel 边缘检测实时滤镜

**验收问题**:
- 卷积和相关的区别?`cv2.filter2D` 做的是哪个?对 Sobel 这种非对称核有影响吗?
- 直方图均衡化为什么用 CDF?什么情况下效果差?
- 高斯核可分离把复杂度从 O(k²) 降到 O(2k) 的原理?
- Unsharp Masking 的三步是什么?和拉普拉斯锐化是什么关系?

**避坑**:滤波在 float 域做,最后 clip;对比 OpenCV 时统一 borderType;Metal 的 `_srgb` 纹理采样返回线性值,在 gamma 空间做模糊会偏暗。

### 阶段 3(第 8~9 周):彩色图像处理 —— 第 6 章 + 视频色彩工程补充

**阅读**:6.1~6.2(色彩基础、颜色模型)、6.4~6.6(全彩处理、彩色变换、彩色平滑锐化)精读;6.3 伪彩色泛读;6.7(基于彩色的分割)留到阶段 5 前回看。

**书外必补**(书里不细讲,但是推流工作的核心):
- YCbCr 的 BT.601 / BT.709 / BT.2020 三套系数;full range 与 video range(Y: 16~235)
- 色度下采样 4:4:4 / 4:2:2 / 4:2:0,以及 NV12 / I420 内存布局
- 参考(选):Poynton《Digital Video and HD》

**Python 实践**:
- [ ] 手写 RGB↔HSV,和 OpenCV 对比(注意 OpenCV 的 H 范围是 0~180)
- [ ] 在亮度通道做直方图均衡(YCbCr 的 Y / HSI 的 I),对比直接对 RGB 三通道均衡的偏色问题
- [ ] 白平衡:灰度世界假设(Gray World)实现
- [ ] 3D LUT:加载 .cube 文件 + 三线性插值应用(直播滤镜的工业标准)

**Metal 实践**:
- [ ] 3D LUT 调色 shader(用 3D 纹理硬件插值)
- [ ] 饱和度 / 色温 / 对比度实时调节

**验收问题**:
- 为什么视频用 YUV 而不是 RGB?4:2:0 省了多少数据量,视觉上为什么可接受?
- 对 RGB 三通道分别做直方图均衡为什么会偏色?
- HSV 和 HSI 的区别?"调饱和度"在两个空间里差在哪?

**避坑**:BT.601/709 系数混用是推流画面偏色的经典 bug。

### 缓冲周(第 10 周):补欠账 + 中期项目

- 前 9 周有任何欠账,这周还清;验收问题全部过一遍。
- **中期项目「实时调色滤镜 App」**:摄像头采集 → YUV→RGB(Metal)→ Gamma/对比度/饱和度 + 3D LUT → 实时直方图叠加显示。只用前三个阶段学到的东西,不引入新知识。
- 效果 GIF 存 `Assets/results/`,README 打卡。

### 阶段 4(第 11~14 周):频域处理 —— 第 4 章(最陡的一段,放慢)

**第 11 周:数学回血(不开书)**
- 复数四则运算、欧拉公式 e^(jθ) = cosθ + j·sinθ(复变函数只需要这一点)
- 3B1B《傅里叶变换的本质》+《线性代数的本质》"基变换"一集(DFT = 换一组基看信号)
- [ ] 手写 1D 朴素 DFT(O(N²) 双层循环),对正弦叠加信号做变换,和 `numpy.fft.fft` 对齐

**第 12~14 周阅读**:4.2、4.4 精读(用 1D 建立直觉);4.3 采样定理泛读(与你的音视频经验挂钩);4.5~4.9 精读(2D DFT、性质、频域滤波、低通/高通);4.10 精读(陷波滤波);4.11 FFT 了解思想即可。

**Python 实践**:
- [ ] `fft2` + `fftshift` + log 显示频谱,对比纹理丰富/平坦图像的频谱差异
- [ ] 验证卷积定理:空域高斯卷积 vs 频域乘高斯传递函数,结果对齐
- [ ] 理想 / 巴特沃斯 / 高斯低通对比(观察理想低通的振铃)
- [ ] 高通锐化;给图像人为叠加周期条纹,用陷波滤波器去除
- [ ] 交换两张图的幅度谱和相位谱重建,体会"相位主导结构"

**Metal 实践(可选)**:频域滤波不适合逐帧实时,本阶段以 Python 为主。

**验收问题**:
- 频谱图中心亮斑是什么?远离中心的点代表什么?
- 卷积定理的工程意义:什么时候频域滤波比空域卷积快?
- 理想低通为什么会振铃(吉布斯现象),高斯低通为什么不会?
- DCT 和 DFT 什么关系?为什么 JPEG/视频编码用 DCT?(为选修 B 埋伏笔)

**避坑**:忘 fftshift / 忘 log(1+|F|) 显示会看到一片黑;实数图像频谱共轭对称;逆变换后取实部前确认虚部接近 0。

### 阶段 5(第 15~17 周):形态学与图像分割 —— 第 9、10 章

**阅读**:
- 第 9 章:9.1~9.3 精读(结构元、腐蚀膨胀、开闭运算);9.5 精读边界提取/孔洞填充/连通分量;灰度形态学(顶帽/底帽)泛读
- 第 10 章:10.2 精读(点线边缘检测,含 Canny 完整流程)、10.3 精读(阈值处理、Otsu);区域生长与 k-means/超像素(第四版新增)泛读;分水岭了解;运动分割一节与视频相关,建议看
- 回看 6.7 基于彩色的分割(为毕业项目做准备)

**Python 实践**:
- [ ] 手写二值腐蚀/膨胀,组合出开/闭运算,清理一张带噪 mask
- [ ] 手写 Otsu(类间方差最大化),对比 `cv2.threshold(..., THRESH_OTSU)`
- [ ] 分步实现 Canny:高斯平滑 → Sobel 梯度 → 非极大值抑制 → 双阈值 + 滞后连接,逐步可视化中间结果
- [ ] 连通分量标记,统计目标个数

**验收问题**:
- 开运算和闭运算分别适合处理什么缺陷?先后顺序为什么不能随便换?
- Otsu 假设直方图是什么形状?什么时候失效(光照不均 → 自适应阈值)?
- Canny 的非极大值抑制解决什么问题?双阈值为什么比单阈值好?

**避坑**:手写 Canny 与 OpenCV 结果不必逐像素一致(实现细节不同),趋势对即可。

### 毕业项目(第 18 周):实时绿幕抠图

综合主线全部知识:
1. Python 原型:HSV/YCbCr 色度键控生成 mask → 开/闭运算清边 → 高斯羽化 → alpha 合成新背景
2. Metal 实时版:摄像头帧 → 色度键控 compute shader → 形态学 pass → 羽化 → 合成,目标 1080p 60fps
3. README 放前后对比 GIF,`Documents/` 写一篇总结(踩坑、性能数据)

完成即主线毕业。

## 四、选修模块(主线完成后按需选择)

### 选修 A:第 5 章 噪声模型与去噪(约 2 周)

读 5.1~5.4:噪声模型(高斯/椒盐等)、均值族滤波器、自适应滤波、周期噪声抑制。
延伸(书外,工程价值高):双边滤波(美颜磨皮基础)、非局部均值、视频时域/时空联合降噪。
适合:关心弱光推流画质。

### 选修 B:第 8 章 图像压缩(约 2~3 周)—— 对推流开发者价值最高的选修

读 8.1(冗余与信息熵)、块变换编码一节(DCT、JPEG 完整流程、量化表、块效应)、预测编码一节(含运动补偿 —— H.264/HEVC 帧间预测的理论原型)。
实践:手写 8×8 DCT + 量化 + 反变换,观察不同量化强度下的块效应;实现简单差分预测编码。
收益:把"码率、量化、块效应、I/P 帧"这些日常名词接到理论根上。

### 选修 C:第 12 章 神经网络与深度学习入门(约 2 周)

第四版新增:感知机 → 多层网络与反向传播 → 深度卷积网络。
意义:现代人像分割/抠图(对比毕业项目的传统绿幕方案)全是 CNN 路线,这章是通往 Vision/CoreML 人像分割的桥。

### 明确跳过

第 7 章小波与其他变换、第 11 章特征提取(SIFT 等)——需要时当手册回来查。

## 五、通用避坑清单

1. **uint8 回绕**:numpy 里 uint8 的 200+100 = 44。所有运算先 `astype(np.float64)`,输出前 `np.clip(x, 0, 255).astype(np.uint8)`。
2. **卷积 vs 相关**:`cv2.filter2D` 实际做的是相关。对称核无所谓;Sobel 这类非对称核要清楚差别。
3. **边界处理**:手写结果与 OpenCV 的差异往往只在图像边缘 —— 先统一 borderType 再对比。
4. **频谱显示**:`log(1 + |F|)` + `fftshift`,否则一片黑。
5. **sRGB 与线性空间**:Metal 的 `_srgb` 纹理采样返回线性值;在 gamma 空间做模糊结果偏暗。做滤波前想清楚在哪个空间。
6. **YUV range**:video range(Y: 16~235)与 full range 的转换矩阵不同;BT.601/709 系数混用会导致肤色偏差 —— 推流最常见的色彩 bug。
7. **别过度 Metal 化**:Python 主线不能停,Metal 只做清单里列出的。

## 六、每周节奏参考

- 工作日 2~3 个晚上 × 1~1.5h:读书 + 写笔记(`Documents/`)
- 周末半天 × 3~4h:写代码、做实验、打卡
- 每阶段结束:过一遍验收问题,答不上来的回头补;README 勾进度

## 七、资源清单

| 资源 | 用途 |
| :--- | :--- |
| 《数字图像处理(第四版)》 | 主教材 |
| imageprocessingplace.com | 教材官方站,下载标准测试图 |
| 3B1B《线性代数的本质》《傅里叶变换的本质》 | 数学直觉(B站/YouTube) |
| OpenCV-Python 官方 Tutorials | 对照参考实现 |
| MetalPerformanceShaders(MPS) | 现成 GaussianBlur/Histogram/Sobel,作为自写 shader 的对照基准 |
| Accelerate / vImage | CPU 端参考基准 |
| Poynton《Digital Video and HD》(选) | 视频色彩权威(YUV/range/色域一手资料) |
| DIPUM 配套代码(选) | 冈萨雷斯官方实践指南 |
